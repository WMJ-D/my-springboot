<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test.mapper.SysRoleMapper">

    <sql id="roleConditions">
        deleted = 0
        <if test="roleName != null and roleName != ''">
            AND role_name LIKE CONCAT('%', #{roleName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </sql>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM sys_role
        <where>
            <include refid="roleConditions"/>
        </where>
    </select>

    <select id="list" resultType="map">
        SELECT id, role_name AS roleName, role_key AS roleKey, data_scope AS dataScope,
               sort_order AS sort, status, remark, created_at AS createdAt, created_at AS createTime
        FROM sys_role
        <where>
            <include refid="roleConditions"/>
        </where>
        ORDER BY sort_order, id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (role_name, role_key, data_scope, sort_order, status, remark, created_by, updated_by)
        VALUES (#{roleName}, #{roleKey}, #{dataScope}, #{sort}, #{status}, #{remark}, #{createdBy}, #{updatedBy})
    </insert>

    <select id="findAdminRoleIds" resultType="long">
        SELECT id FROM sys_role WHERE role_key = 'admin' AND id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>

    <update id="softDeleteByIds">
        UPDATE sys_role SET deleted = 1, updated_by = #{updatedBy}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </update>

    <insert id="insertRoleMenus">
        INSERT INTO sys_role_menu (role_id, menu_id) VALUES
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId})
        </foreach>
    </insert>

</mapper>
