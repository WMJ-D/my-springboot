<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test.mapper.SysAppMapper">

    <sql id="appConditions">
        deleted = 0
        <if test="appId != null and appId != ''">
            AND app_id LIKE CONCAT('%', #{appId}, '%')
        </if>
        <if test="appName != null and appName != ''">
            AND app_name LIKE CONCAT('%', #{appName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </sql>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM sys_app
        <where>
            <include refid="appConditions"/>
        </where>
    </select>

    <select id="list" resultType="map">
        SELECT id, app_id AS appId, app_name AS appName, base_url AS baseUrl, icon,
               sort_order AS sort, status, remark,
               created_at AS createdAt, created_at AS createTime
        FROM sys_app
        <where>
            <include refid="appConditions"/>
        </where>
        ORDER BY sort_order, id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_app (app_id, app_name, base_url, icon, sort_order, status, remark, created_by, updated_by)
        VALUES (#{appId}, #{appName}, #{baseUrl}, #{icon}, #{sort}, #{status}, #{remark}, #{createdBy}, #{updatedBy})
    </insert>

    <update id="softDeleteByIds">
        UPDATE sys_app SET deleted = 1, updated_by = #{updatedBy}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </update>

</mapper>
